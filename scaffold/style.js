import { style } from "som-style";
// import { theme } from "./theme.js";
// import { space } from "./constant.js";

/**
 * Optional example only — put page/component styles wherever you prefer
 * (e.g. src/styles/, next to components). Not required by som-style.
 *
 * export const card = style({
 *   base: { padding: space.s4, color: theme.text },
 * });
 */
